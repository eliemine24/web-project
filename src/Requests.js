/* http iTunes api requests maker from "id" and "name" argument */

async function requestMaker(id="", name="") {

    let req = "";
    if (listeArtistes.has(id)) {
        // build http request to get a song from the artists "id" from the iTunes api
        // store it in req
    }

    if (listeArtistes.has(id)) {
        // build http request to get a song from the artists "id" from the iTunes api
        // store it in req
    }

    return req;
}